apiVersion: v1
kind: Service
metadata:
  name: customer-churn-service
spec:
  selector:
    app: customer-churn
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
